--- ./9.c
+++ ./9.c._WAEP_2	2016-12-08 00:51:12.491083186 +0000
@@ -35,7 +35,7 @@
     printTable(tableColumns, lines, columns);
     for (i = 0;i < lines;i++){
         free(table[i]);
-        free(tableLines[i]);
+        free(tableLines);
         free(tableColumns[i]);
     }
     free(table);
