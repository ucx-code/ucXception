--- ./9.c
+++ ./9.c._MLPA_21	2016-12-08 00:51:10.719083218 +0000
@@ -38,9 +38,6 @@
         free(tableLines[i]);
         free(tableColumns[i]);
     }
-    free(table);
-    free(tableLines);
-    free(tableColumns);
     return 0;
 }
 
