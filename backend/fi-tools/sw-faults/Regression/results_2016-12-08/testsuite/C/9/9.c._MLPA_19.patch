--- ./9.c
+++ ./9.c._MLPA_19	2016-12-08 00:51:10.671083219 +0000
@@ -38,8 +38,6 @@
         free(tableLines[i]);
         free(tableColumns[i]);
     }
-    free(table);
-    free(tableLines);
     free(tableColumns);
     return 0;
 }
