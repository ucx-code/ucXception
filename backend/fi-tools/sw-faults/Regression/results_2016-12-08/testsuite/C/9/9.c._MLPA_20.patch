--- ./9.c
+++ ./9.c._MLPA_20	2016-12-08 00:51:10.703083218 +0000
@@ -39,8 +39,6 @@
         free(tableColumns[i]);
     }
     free(table);
-    free(tableLines);
-    free(tableColumns);
     return 0;
 }
 
