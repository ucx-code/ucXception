--- ./10.c
+++ ./10.c._MFC_32	2016-12-08 00:49:33.039084967 +0000
@@ -138,7 +138,7 @@
         type = get_type(node->child->id, prog, method_name);
         expr = gen_expression(method_name, node->child->next, prog);
         if (!glob) 
-            printf("\tstore %s %s, %s* %%%s\n", type, expr, type, node->child->id);
+            ;
         else
             printf("\tstore %s %s, %s* @%s\n", type, expr, type, node->child->id);
 
