--- ./24.c
+++ ./24.c._MLPA_1	2016-12-08 00:50:20.379084119 +0000
@@ -55,8 +55,6 @@
         rec(0, n, r % 4);
         for (i = 0;i < pow(2, n);i++){
             memcpy(tmp, matrix + (int)(i * pow(2, n)), pow(2, n));
-            tmp[(int)pow(2, n)] = '\0';
-            printf("%s\n", tmp);
         }
     }
 }
