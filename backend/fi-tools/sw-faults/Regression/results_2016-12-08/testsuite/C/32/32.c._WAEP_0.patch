--- ./32.c
+++ ./32.c._WAEP_0	2016-12-08 00:50:45.087083677 +0000
@@ -9,7 +9,7 @@
     if (lvl == 0) {
         matrix[0][0] = '0';
     }else{
-        gerarMatrix(lvl - 1, rot);
+        gerarMatrix(lvl, rot);
         aux1 = pow(2, lvl);
         aux2 = pow(2, (lvl - 1));
         if (rot == 0) {
