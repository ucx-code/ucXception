--- ./32.c
+++ ./32.c._MLPA_7	2016-12-08 00:50:45.619083667 +0000
@@ -75,8 +75,6 @@
 {
     short rot, lvl, size, i;
     while (scanf("%hd %hd", &lvl, &rot) != EOF){
-        rot = rot % 4;
-        size = pow(2, lvl);
         gerarMatrix(lvl, rot);
         for (i = 0;i < size;i++){
             matrix[i][size] = '\0';
