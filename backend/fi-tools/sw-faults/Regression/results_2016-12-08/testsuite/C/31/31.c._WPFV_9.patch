--- ./31.c
+++ ./31.c._WPFV_9	2016-12-08 00:50:42.999083714 +0000
@@ -41,7 +41,7 @@
                 memcpy(&tab[i + comp][0], &tab[i][0], comp * sizeof (char));
                 memcpy(&tab[i + comp][comp], &tab[i][0], comp * sizeof (char));
             }
-            invert(0, comp, comp);
+            invert(0, N, comp);
         }else
             if (R == 2) {
                 for (i = 0;i < comp;i++){
