--- ./31.c
+++ ./31.c._WAEP_4	2016-12-08 00:50:42.787083718 +0000
@@ -32,7 +32,7 @@
     if (R == 0) {
         for (i = 0;i < comp;i++){
             memcpy(&tab[i][comp], &tab[i][0], comp * sizeof (char));
-            memcpy(&tab[i + comp][0], &tab[i][0], comp * sizeof (char));
+            memcpy(&tab[i + comp][0], &tab[i][0], comp);
         }
         invert(comp, comp, comp);
     }else
