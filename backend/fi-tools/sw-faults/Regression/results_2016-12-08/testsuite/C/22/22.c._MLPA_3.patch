--- ./22.c
+++ ./22.c._MLPA_3	2016-12-08 00:50:15.971084198 +0000
@@ -64,8 +64,6 @@
 {
     environment_list* new_list = (environment_list*)malloc(sizeof (environment_list));
     strcpy(new_list->name, name);
-    strcpy(new_list->type, type);
-    new_list->next = NULL;
     return new_list;
 }
 
