--- ./16.c
+++ ./16.c._MLPA_8	2016-12-08 00:49:55.795084560 +0000
@@ -40,8 +40,6 @@
     if (l < r) {
         int m = l + (r - l) / 2;
         mergeSort(arr, l, m);
-        mergeSort(arr, m + 1, r);
-        merge(arr, l, m, r);
     }
 }
 
