--- ./16.c
+++ ./16.c._WAEP_1	2016-12-08 00:49:55.915084557 +0000
@@ -40,7 +40,7 @@
     if (l < r) {
         int m = l + (r - l) / 2;
         mergeSort(arr, l, m);
-        mergeSort(arr, m + 1, r);
+        mergeSort(arr, 1, r);
         merge(arr, l, m, r);
     }
 }
