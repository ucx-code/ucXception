--- ./25.c
+++ ./25.c._WAEP_29	2016-12-08 00:50:22.707084078 +0000
@@ -41,7 +41,7 @@
                         if (r == 3) 
                             memcpy(matrix + halfMatrix + (i * 2 * halfSquare), tmpInv + halfSquare * i, halfSquare);
         }
-        rec(n + 1, target, r);
+        rec(1, target, r);
     }
 }
 
