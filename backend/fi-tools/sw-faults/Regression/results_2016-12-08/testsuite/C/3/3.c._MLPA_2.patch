--- ./3.c
+++ ./3.c._MLPA_2	2016-12-08 00:50:37.255083817 +0000
@@ -54,8 +54,6 @@
         rec(0, n, r % 4);
         for (i = 0;i < squared;i++){
             memcpy(tmp, matrix + (i * squared), squared);
-            tmp[squared] = '\0';
-            printf("%s\n", tmp);
         }
     }
 }
