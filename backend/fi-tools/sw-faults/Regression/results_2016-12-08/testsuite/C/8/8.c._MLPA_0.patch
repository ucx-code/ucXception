--- ./8.c
+++ ./8.c._MLPA_0	2016-12-08 00:51:07.687083272 +0000
@@ -9,8 +9,6 @@
     int nString, i, tam = 0;
     char** pWord;
     char *numStrings, *actWord, *finalWord;
-    numStrings = (char*)malloc(5 * sizeof (char));
-    scanf("%s", numStrings);
     while (checkWord(numStrings) == 1)
         scanf("%s", numStrings);
     nString = atoi(numStrings);
