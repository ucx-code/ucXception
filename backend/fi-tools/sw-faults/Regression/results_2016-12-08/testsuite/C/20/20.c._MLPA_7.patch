--- ./20.c
+++ ./20.c._MLPA_7	2016-12-08 00:50:09.155084320 +0000
@@ -53,10 +53,8 @@
 while (node != NULL){
     switch (node->type){
         case MethodParams:
-            check_methodParams(prog, method_name, node->child);
             break;
         case MethodBody:
-            check_methodBody(prog, method_name, node->child);
             break;
         default:
             break;
