--- ./21.c
+++ ./21.c._MIFS_0	2016-12-08 00:50:14.139084231 +0000
@@ -15,9 +15,6 @@
         else
             printf("%s\n", types[node->type]);
     }
-    if (node->child) 
-        printAST(node->child, tabs + 1);
-
     if (node->next) 
         printAST(node->next, tabs);
 }
