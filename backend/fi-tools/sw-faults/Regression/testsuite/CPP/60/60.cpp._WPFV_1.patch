--- 60.cpp
+++ 60.cpp._WPFV_1	2016-03-30 01:41:46.287588000 +0100
@@ -64,7 +64,7 @@
     for (i = 0;i < n;i++){
         scanf("%d %d", &slot[i].begin, &slot[i].end);
     }
-    qsort(slot, n, sizeof (struct _slot), compare);
+    qsort(slot, a, sizeof (struct _slot), compare);
     a[1] = slot[0].end;
     for (i = 1;i < n;i++){
         if (a[1] <= slot[i].begin) {
