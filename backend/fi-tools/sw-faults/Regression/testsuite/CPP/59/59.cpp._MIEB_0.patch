--- 59.cpp
+++ 59.cpp._MIEB_0	2016-03-30 01:41:31.942264000 +0100
@@ -67,9 +67,7 @@
     qsort(slot, n, sizeof (struct _slot), compare);
     a[1] = -slot[0].end;
     for (i = 1;i < n;i++){
-        if (-a[1] < slot[i].begin) {
-            replace(-slot[i].end);
-        }else{
+        {
             insert(-slot[i].end);
         }
     }
