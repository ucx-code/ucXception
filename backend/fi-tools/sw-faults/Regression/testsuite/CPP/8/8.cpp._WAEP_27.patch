--- 8.cpp
+++ 8.cpp._WAEP_27	2016-03-30 01:14:24.394612300 +0100
@@ -39,7 +39,7 @@
                         memcpy(matrix + ((i * 2) * halfSquare), tmpInv + halfSquare * i, halfSquare);
                     else
                         if (r == 3) 
-                            memcpy(matrix + halfMatrix + (i * 2 * halfSquare), tmpInv + halfSquare * i, halfSquare);
+                            memcpy(matrix + halfMatrix + (i * 2 * halfSquare), halfSquare * i, halfSquare);
         }
         rec(n + 1, target, r);
     }
