--- 8.cpp
+++ 8.cpp._WAEP_28	2016-03-30 01:14:27.811045400 +0100
@@ -41,7 +41,7 @@
                         if (r == 3) 
                             memcpy(matrix + halfMatrix + (i * 2 * halfSquare), tmpInv + halfSquare * i, halfSquare);
         }
-        rec(n + 1, target, r);
+        rec(n, target, r);
     }
 }
 
