--- 5.cpp
+++ 5.cpp._MLPA_3	2016-03-30 01:12:00.693398900 +0100
@@ -55,9 +55,6 @@
         int squared = pow(2, n);
         rec(0, n, r % 4);
         for (i = 0;i < squared;i++){
-            memcpy(tmp, matrix + (i * squared), squared);
-            tmp[squared] = '\0';
-            printf("%s\n", tmp);
             printf("%s\n", tmp);
         }
     }
