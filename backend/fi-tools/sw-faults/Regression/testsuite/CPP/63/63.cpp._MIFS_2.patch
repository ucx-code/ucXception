--- 63.cpp
+++ 63.cpp._MIFS_2	2016-03-30 01:42:22.571346800 +0100
@@ -28,9 +28,6 @@
 
         swap(*ll, *rr);
     }
-    if (!(bit & *ll) && ll < to) 
-        ll++;
-
     bit >>= 1;
     rad_sort_u(from, ll, bit);
     rad_sort_u(ll, to, bit);
