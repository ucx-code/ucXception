--- 63.cpp
+++ 63.cpp._MIFS_1	2016-03-30 01:42:19.981517900 +0100
@@ -23,9 +23,6 @@
             ll++;
         while (ll < rr && (*rr & bit))
             rr--;
-        if (ll >= rr) 
-            break;
-
         swap(*ll, *rr);
     }
     if (!(bit & *ll) && ll < to) 
