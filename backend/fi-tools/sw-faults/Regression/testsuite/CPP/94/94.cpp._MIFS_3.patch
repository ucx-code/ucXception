--- 94.cpp
+++ 94.cpp._MIFS_3	2016-03-30 02:02:54.956192300 +0100
@@ -36,9 +36,8 @@
         bipartite = true;
         memset(mark, false, sizeof (bool) * n);
         memset(color, 0, sizeof (int) * n);
-        for (i = 0;i < n;i++)
-            if (mark[i] == false) 
-                dfs(i, 1);
+        for (i = 0;i < n;i++);
+
         for (i = 0;i <= n;i++)
             memset(a[i], 0, sizeof (int) * n);
         if (bipartite == true) 
