--- 55.cpp
+++ 55.cpp._MFC_2	2016-03-30 01:40:39.740410900 +0100
@@ -53,7 +53,7 @@
     struct _slotLL* head = newSlot(slot[0].begin, slot[0].end);
     for (i = n;i > 0;i--){
         struct _slotLL* tmp = newSlot(slot[i].begin, slot[i].end);
-        appendSlot(head, tmp);
+        ;
     }
     if (head->next != NULL) 
         head->next->parent = head;
