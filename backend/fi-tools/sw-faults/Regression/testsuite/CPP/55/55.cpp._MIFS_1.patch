--- 55.cpp
+++ 55.cpp._MIFS_1	2016-03-30 01:40:39.996943600 +0100
@@ -55,9 +55,6 @@
         struct _slotLL* tmp = newSlot(slot[i].begin, slot[i].end);
         appendSlot(head, tmp);
     }
-    if (head->next != NULL) 
-        head->next->parent = head;
-
     struct _slotLL* master = head;
     struct _slotLL* actual = master->next;
     int conta;
