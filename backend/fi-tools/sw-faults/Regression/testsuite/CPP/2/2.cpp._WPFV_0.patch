--- 2.cpp
+++ 2.cpp._WPFV_0	2016-03-30 01:10:31.544310700 +0100
@@ -121,7 +121,7 @@
             break;
 
         matrixSize = (width + 1) * height;
-        fread(buffer, 1, matrixSize, stdin);
+        fread(buffer, 1, width, stdin);
         for (i = matrixSize - width - 1;i < matrixSize;i++){
             if ((buffer[i] == '|') || (buffer[i] == '\\') || (buffer[i] == '/')) {
                 recurseSize = 0;
