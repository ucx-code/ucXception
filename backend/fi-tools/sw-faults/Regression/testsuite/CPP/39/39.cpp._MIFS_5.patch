--- 39.cpp
+++ 39.cpp._MIFS_5	2016-03-30 01:36:36.948720400 +0100
@@ -46,15 +46,6 @@
                 return 1;
             }
         }
-        if (bufferPosWidth1 == '|') {
-            if (recurse(pos - width - 1, max, width) == 1) {
-                if (recurseSize != 0) 
-                    printf(",");
-
-                printf("(%d,%d)", getY(pos, width), getX(pos, width));
-                return 1;
-            }
-        }
         if (bufferPosWidth2 == '\\') {
             if (recurse(pos - width - 2, max, width) == 1) {
                 if (recurseSize != 0) 
