--- 39.cpp
+++ 39.cpp._MIA_5	2016-03-30 01:36:34.642927500 +0100
@@ -39,9 +39,7 @@
     if (bufferPos == '|') {
         if (bufferPosWidth == '/') {
             if (recurse(pos - width, max, width) == 1) {
-                if (recurseSize != 0) 
                     printf(",");
-
                 printf("(%d,%d)", getY(pos, width), getX(pos, width));
                 return 1;
             }
