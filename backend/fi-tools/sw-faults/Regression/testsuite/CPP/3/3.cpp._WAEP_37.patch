--- 3.cpp
+++ 3.cpp._WAEP_37	2016-03-30 01:11:08.980068500 +0100
@@ -43,7 +43,7 @@
                 memcpy(matrix + (halfSquare * 2 * halfSquare) + ((i * 2 + 1) * halfSquare), tmp + halfSquare * i, halfSquare);
             }
         }
-        rec(n + 1, target, r);
+        rec(1, target, r);
     }
 }
 
