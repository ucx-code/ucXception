--- 93.cpp
+++ 93.cpp._WAEP_3	2016-03-30 02:02:37.743446800 +0100
@@ -35,7 +35,7 @@
             vizinhos[b][vizinhosC[b]++] = a;
         }
         is = true;
-        memset(marked, 0, sizeof (bool) * n);
+        memset(marked, 0, n);
         memset(color, 0, sizeof (bool) * n);
         for (i = 0;i < n;i++){
             if (!marked[i]) 
