--- 99.cpp
+++ 99.cpp._MLPA_1574	2016-03-30 02:41:53.341253700 +0100
@@ -890,8 +890,6 @@
     float len;
     x = (2.0 * ((GLfloat)rand()) / ((GLfloat)RAND_MAX)) - 1.0;
     y = (2.0 * ((GLfloat)rand()) / ((GLfloat)RAND_MAX)) - 1.0;
-    z = (2.0 * ((GLfloat)rand()) / ((GLfloat)RAND_MAX)) - 1.0;
-    len = sqrt(x * x + y * y + z * z);
     if (len) {
         x = x / len;
         y = y / len;
