--- 99.cpp
+++ 99.cpp._WVAV_10	2016-03-30 02:56:46.350521600 +0100
@@ -1654,7 +1654,7 @@
             pontos = 0;
             instrucoes = 0;
             acerca = 0;
-            iniciarjogo = 1;
+            iniciarjogo = 0 ^ 0xff;
             glutSetCursor (GLUT_CURSOR_LEFT_ARROW);
         }else
             if ((opcoes == 0) && (x > 150) && (x < 350) && (y > 190) && (y < 250) && (iniciarjogo != 1)) {
