--- 99.cpp
+++ 99.cpp._MLPA_1581	2016-03-30 02:41:57.489281400 +0100
@@ -893,8 +893,6 @@
     z = (2.0 * ((GLfloat)rand()) / ((GLfloat)RAND_MAX)) - 1.0;
     len = sqrt(x * x + y * y + z * z);
     if (len) {
-        x = x / len;
-        y = y / len;
         z = z / len;
     }
     dest[0] = x;
