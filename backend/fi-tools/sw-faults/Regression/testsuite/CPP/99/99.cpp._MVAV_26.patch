--- 99.cpp
+++ 99.cpp._MVAV_26	2016-03-30 02:57:46.689585300 +0100
@@ -1681,7 +1681,6 @@
                         pontos = 0;
                         instrucoes = 1;
                         acerca = 0;
-                        iniciarjogo = 0;
                         glutSetCursor (GLUT_CURSOR_LEFT_ARROW);
                     }else
                         if ((acerca == 0) && (x > 150) && (x < 350) && (y > 435) && (y < 470) && (iniciarjogo != 1)) {
