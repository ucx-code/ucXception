--- 99.cpp
+++ 99.cpp._MVAE_4	2016-03-30 02:41:58.066354300 +0100
@@ -894,7 +894,6 @@
     len = sqrt(x * x + y * y + z * z);
     if (len) {
         x = x / len;
-        y = y / len;
         z = z / len;
     }
     dest[0] = x;
