--- 99.cpp
+++ 99.cpp._WVAV_28	2016-03-30 02:57:47.081134800 +0100
@@ -1681,7 +1681,7 @@
                         pontos = 0;
                         instrucoes = 1;
                         acerca = 0;
-                        iniciarjogo = 0;
+                        iniciarjogo = 0 ^ 0xff;
                         glutSetCursor (GLUT_CURSOR_LEFT_ARROW);
                     }else
                         if ((acerca == 0) && (x > 150) && (x < 350) && (y > 435) && (y < 470) && (iniciarjogo != 1)) {
