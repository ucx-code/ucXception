--- 99.cpp
+++ 99.cpp._MVAV_15	2016-03-30 02:57:15.131574600 +0100
@@ -1667,7 +1667,6 @@
                 glutSetCursor (GLUT_CURSOR_LEFT_ARROW);
             }else
                 if ((pontos == 0) && (x > 150) && (x < 350) && (y > 280) && (y < 320) && (iniciarjogo != 1)) {
-                    inicio = 0;
                     opcoes = 0;
                     pontos = 1;
                     instrucoes = 0;
