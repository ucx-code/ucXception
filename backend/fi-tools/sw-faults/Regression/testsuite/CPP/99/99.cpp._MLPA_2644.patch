--- 99.cpp
+++ 99.cpp._MLPA_2644	2016-03-30 02:57:12.144194000 +0100
@@ -1669,10 +1669,6 @@
                 if ((pontos == 0) && (x > 150) && (x < 350) && (y > 280) && (y < 320) && (iniciarjogo != 1)) {
                     inicio = 0;
                     opcoes = 0;
-                    pontos = 1;
-                    instrucoes = 0;
-                    acerca = 0;
-                    iniciarjogo = 0;
                     glutSetCursor (GLUT_CURSOR_LEFT_ARROW);
                 }else
                     if ((instrucoes == 0) && (x > 150) && (x < 350) && (y > 360) && (y < 395) && (iniciarjogo != 1)) {
