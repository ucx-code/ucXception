--- 99.cpp
+++ 99.cpp._MLPA_2808	2016-03-30 03:00:51.473224000 +0100
@@ -1885,10 +1885,7 @@
     glClearColor(PRETO, , , );
     glMatrixMode (GL_PROJECTION);
     glLoadIdentity();
-    glOrtho(-10.0, 10.0, -10.0, 10.0, 0.0, 150.0);
     glMatrixMode (GL_MODELVIEW);
-    glLoadIdentity();
-    sprintf(textoPontos, "%d", score);
     if (inicio == 1) {
         menu();
     }else
