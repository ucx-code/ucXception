--- 99.cpp
+++ 99.cpp._MLPA_1096	2016-03-30 02:25:56.197422600 +0100
@@ -692,11 +692,6 @@
     glTexCoord2f(1.0f, 0.0f);
     glVertex2f(5.0f, 0.0f);
     glTexCoord2f(0.0f, 0.0f);
-    glVertex2f(-5.0f, 0.0f);
-    glEnd();
-    glPopMatrix();
-    glDisable(GL_TEXTURE_2D);
-    glEnable(GL_TEXTURE_2D);
     glBindTexture(GL_TEXTURE_2D, texture[4]);
     glPushMatrix();
     glBegin(GL_QUADS);
