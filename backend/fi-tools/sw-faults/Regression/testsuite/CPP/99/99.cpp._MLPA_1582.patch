--- 99.cpp
+++ 99.cpp._MLPA_1582	2016-03-30 02:41:57.663303000 +0100
@@ -894,8 +894,6 @@
     len = sqrt(x * x + y * y + z * z);
     if (len) {
         x = x / len;
-        y = y / len;
-        z = z / len;
     }
     dest[0] = x;
     dest[1] = y;
