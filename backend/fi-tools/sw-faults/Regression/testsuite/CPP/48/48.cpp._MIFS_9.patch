--- 48.cpp
+++ 48.cpp._MIFS_9	2016-03-30 01:39:12.204711700 +0100
@@ -84,16 +84,6 @@
                 return 1;
             }
         }
-        if (bufferPosWidth2 == '\\') {
-            if (recurse(pos - width - 2, max, width) == 1) {
-                if (recurseSize != 0) 
-                    strcat2((char*)",", 1);
-
-                sprintf(tmp, "(%d,%d)", getY(pos, width), getX(pos, width));
-                strcat2(tmp);
-                return 1;
-            }
-        }
     }
     if ((bufferPos == '\\')) {
         if ((bufferPosWidth2 == '|') || (bufferPosWidth2 == '\\')) {
