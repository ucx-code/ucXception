--- 48.cpp
+++ 48.cpp._MIFS_10	2016-03-30 01:39:12.843292600 +0100
@@ -86,9 +86,6 @@
         }
         if (bufferPosWidth2 == '\\') {
             if (recurse(pos - width - 2, max, width) == 1) {
-                if (recurseSize != 0) 
-                    strcat2((char*)",", 1);
-
                 sprintf(tmp, "(%d,%d)", getY(pos, width), getX(pos, width));
                 strcat2(tmp);
                 return 1;
