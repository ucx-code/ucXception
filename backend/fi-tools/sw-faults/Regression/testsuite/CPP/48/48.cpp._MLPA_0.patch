--- 48.cpp
+++ 48.cpp._MLPA_0	2016-03-30 01:38:55.407076600 +0100
@@ -10,8 +10,6 @@
 void strcat2(char* text)
 {
     int x = strlen(text);
-    memcpy(outputBuffer + counter, text, x);
-    counter += x;
     if (counter >= 7000) {
         fwrite(outputBuffer, 1, counter, stdout);
         outputBuffer[0] = '\0';
