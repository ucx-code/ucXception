--- 18.cpp
+++ 18.cpp._MLPA_0	2016-03-30 01:20:17.646017300 +0100
@@ -70,8 +70,6 @@
         }
         if ((bufferPos1 == '/') && (bufferPos2 == '\\')) {
             if (recurse(pos + 2, max, width) == 1) {
-                answer.push(pos + 1);
-                answer.push(pos);
                 return 1;
             }
         }
