--- 10.cpp
+++ 10.cpp._MLPA_0	2016-03-30 01:15:17.939707000 +0100
@@ -56,8 +56,6 @@
     if ((bufferPos == '\\')) {
         if ((bufferPos1 == '/') && (bufferPos2 == '\\')) {
             if (recurse(pos + 2, max, width) == 1) {
-                answer.push(pos + 1);
-                answer.push(pos);
                 return 1;
             }
         }
