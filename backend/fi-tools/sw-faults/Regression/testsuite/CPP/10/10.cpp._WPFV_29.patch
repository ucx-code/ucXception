--- 10.cpp
+++ 10.cpp._WPFV_29	2016-03-30 01:15:17.089599100 +0100
@@ -49,7 +49,7 @@
     }
     if (((bufferPos == '\\') && (bufferPosWidth2 == '|')) || ((bufferPos == '\\') && (bufferPosWidth2 == '\\')) || ((bufferPos == '|') && (bufferPosWidth2 == '\\'))) {
         if (recurse(pos + width + 2, max, width) == 1) {
-            answer.push(pos);
+            answer.push(bufferPos2);
             return 1;
         }
     }
