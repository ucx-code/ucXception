--- 2.c
+++ 2.c._MLPA_1	2016-03-30 00:29:22.862627400 +0100
@@ -55,8 +55,6 @@
         rec(0, n, r % 4);
         for (i = 0;i < pow(2, n);i++){
             memcpy(tmp, matrix + (int)(i * pow(2, n)), pow(2, n));
-            tmp[(int)pow(2, n)] = '\0';
-            printf("%s\n", tmp);
         }
     }
 }
