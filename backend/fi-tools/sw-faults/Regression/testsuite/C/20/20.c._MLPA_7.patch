--- 20.c
+++ 20.c._MLPA_7	2016-03-30 00:47:13.664185900 +0100
@@ -53,10 +53,8 @@
 while (node != NULL){
     switch (node->type){
         case MethodParams:
-            check_methodParams(prog, method_name, node->child);
             break;
         case MethodBody:
-            check_methodBody(prog, method_name, node->child);
             break;
         default:
             break;
