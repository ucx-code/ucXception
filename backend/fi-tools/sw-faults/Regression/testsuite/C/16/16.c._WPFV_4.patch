--- 16.c
+++ 16.c._WPFV_4	2016-03-30 00:43:14.912359700 +0100
@@ -40,7 +40,7 @@
     if (l < r) {
         int m = l + (r - l) / 2;
         mergeSort(arr, l, m);
-        mergeSort(arr, m + 1, r);
+        mergeSort(arr, m + 1, l);
         merge(arr, l, m, r);
     }
 }
