--- 24.c
+++ 24.c._MFC_11	2016-03-30 00:52:12.946295100 +0100
@@ -56,7 +56,7 @@
         for (i = 0;i < pow(2, n);i++){
             memcpy(tmp, matrix + (int)(i * pow(2, n)), pow(2, n));
             tmp[(int)pow(2, n)] = '\0';
-            printf("%s\n", tmp);
+            ;
         }
     }
 }
