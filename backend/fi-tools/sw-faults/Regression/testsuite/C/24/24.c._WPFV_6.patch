--- 24.c
+++ 24.c._WPFV_6	2016-03-30 00:51:57.579515300 +0100
@@ -43,7 +43,7 @@
                 memcpy(matrix + (halfSquare * 2 * halfSquare) + ((i * 2 + 1) * halfSquare), tmp + halfSquare * i, halfSquare);
             }
         }
-        rec(n + 1, target, r);
+        rec(n + 1, halfSquare, r);
     }
 }
 
