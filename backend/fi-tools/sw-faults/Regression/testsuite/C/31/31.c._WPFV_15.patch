--- 31.c
+++ 31.c._WPFV_15	2016-03-30 00:59:21.240023700 +0100
@@ -49,7 +49,7 @@
                     memcpy(&tab[i + comp][0], &tab[i][0], comp * sizeof (char));
                     memcpy(&tab[i + comp][comp], &tab[i][0], comp * sizeof (char));
                 }
-                invert(0, 0, comp);
+                invert(0, 0, N);
             }else
                 if (R == 3) {
                     for (i = 0;i < comp;i++){
