--- 31.c
+++ 31.c._WPFV_20	2016-03-30 00:59:30.896261800 +0100
@@ -56,7 +56,7 @@
                         memcpy(&tab[i][comp], &tab[i][0], comp * sizeof (char));
                         memcpy(&tab[i + comp][comp], &tab[i][0], comp * sizeof (char));
                     }
-                    invert(comp, 0, comp);
+                    invert(i, 0, comp);
                 }
 
     if (comp * 2 == pow(2, N)) 
