--- 32.c
+++ 32.c._WPFV_16	2016-03-30 01:00:30.291810400 +0100
@@ -77,7 +77,7 @@
     while (scanf("%hd %hd", &lvl, &rot) != EOF){
         rot = rot % 4;
         size = pow(2, lvl);
-        gerarMatrix(lvl, rot);
+        gerarMatrix(lvl, lvl);
         for (i = 0;i < size;i++){
             matrix[i][size] = '\0';
             printf("%s\n", matrix[i]);
