--- 32.c
+++ 32.c._MLPA_9	2016-03-30 01:00:22.438312000 +0100
@@ -75,9 +75,6 @@
 {
     short rot, lvl, size, i;
     while (scanf("%hd %hd", &lvl, &rot) != EOF){
-        rot = rot % 4;
-        size = pow(2, lvl);
-        gerarMatrix(lvl, rot);
         for (i = 0;i < size;i++){
             matrix[i][size] = '\0';
             printf("%s\n", matrix[i]);
