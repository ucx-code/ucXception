--- 9.c
+++ 9.c._MLPA_19	2016-03-30 00:31:24.290950900 +0100
@@ -38,8 +38,6 @@
         free(tableLines[i]);
         free(tableColumns[i]);
     }
-    free(table);
-    free(tableLines);
     free(tableColumns);
     return 0;
 }
