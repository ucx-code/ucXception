--- 9.c
+++ 9.c._WAEP_3	2016-03-30 00:32:07.791467000 +0100
@@ -35,7 +35,7 @@
     printTable(tableColumns, lines, columns);
     for (i = 0;i < lines;i++){
         free(table[i]);
-        free(tableLines[i]);
+        free(i);
         free(tableColumns[i]);
     }
     free(table);
