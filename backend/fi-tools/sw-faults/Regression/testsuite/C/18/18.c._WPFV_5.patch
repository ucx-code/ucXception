--- 18.c
+++ 18.c._WPFV_5	2016-03-30 00:44:34.914245200 +0100
@@ -32,7 +32,7 @@
         return;
     }
     if (v != 0) 
-        shortestPath(0, len + distancias[v][0], maxCap, bound);
+        shortestPath(0, len + distancias[v][0], len, bound);
 
     for (i = 1;i <= n;i++){
         if (solution[i] == 0) {
