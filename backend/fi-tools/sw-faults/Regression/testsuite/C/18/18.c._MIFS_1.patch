--- 18.c
+++ 18.c._MIFS_1	2016-03-30 00:44:20.877891400 +0100
@@ -27,10 +27,6 @@
     if ((aux + (bound - distancias[v][14]) / 2) >= best) 
         return;
 
-    if (isSolution() && aux < best) {
-        best = aux;
-        return;
-    }
     if (v != 0) 
         shortestPath(0, len + distancias[v][0], maxCap, bound);
 
