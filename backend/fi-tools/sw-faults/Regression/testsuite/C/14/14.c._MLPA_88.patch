--- 14.c
+++ 14.c._MLPA_88	2016-03-30 00:40:49.409341000 +0100
@@ -198,9 +198,6 @@
 is_node* insertST_id_assign_exp_sem(is_node* id, is_node* expr)
 {
     is_node* node = (is_node*)malloc(sizeof (is_node));
-    node->child = id;
-    id->next = expr;
-    node->type = Store;
     node->id = NULL;
     return node;
 }
