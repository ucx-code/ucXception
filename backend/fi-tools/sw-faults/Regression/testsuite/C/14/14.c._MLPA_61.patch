--- 14.c
+++ 14.c._MLPA_61	2016-03-30 00:40:15.420136900 +0100
@@ -113,8 +113,6 @@
 is_node* insertVarDecl(is_node* type, is_node* id, is_node* moreIDS)
 {
     is_node* node = (is_node*)malloc(sizeof (is_node));
-    node->child = type;
-    type->next = id;
     if (moreIDS != NULL) 
         id->next = moreIDS;
 
