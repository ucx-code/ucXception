--- 14.c
+++ 14.c._MLPA_66	2016-03-30 00:40:20.267753900 +0100
@@ -127,9 +127,6 @@
     is_node* node = (is_node*)malloc(sizeof (is_node));
     if (stat != NULL) {
         if (stat->next != NULL) {
-            node->child = stat;
-            node->next = NULL;
-            node->type = CompoundStat;
             node->id = NULL;
         }else
             return stat;
