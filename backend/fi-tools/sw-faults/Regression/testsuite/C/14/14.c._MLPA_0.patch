--- 14.c
+++ 14.c._MLPA_0	2016-03-30 00:39:06.500914200 +0100
@@ -6,8 +6,6 @@
 is_node* insertProgram(is_node* progbody)
 {
     is_node* node = (is_node*)malloc(sizeof (is_node));
-    node->child = progbody;
-    node->next = NULL;
     node->type = Program;
     node->id = NULL;
     return node;
