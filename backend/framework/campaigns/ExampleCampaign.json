{
	"campaign_name": "Campaign script based",
	"campaign_file_name": "ExampleCampaign.py",
	"campaign_class_name": "ExampleCampaign",
	"configuration": {
		"setup_script_path" : {
			"name": "Setup script path",
			"regex" : "^[a-zA-Z0-9_]+(\\.)sh$",
			"require": true,
			"information_help": "Uploaded file must be a script. Script perform setup for the experiment."
		},
		"workload_script_path": {
			"name": "Workload script path",
			"regex" : "^[a-zA-Z0-9_]+(\\.)sh$",
			"require": true,
			"information_help": "Uploaded file must be a script. Script execute workload."
		},
		"postworkload_script_path": {
			"name": "Post-Workload script path",
			"regex" : "^[a-zA-Z0-9_]+(\\.)sh$",
			"require": true,
			"information_help": "Uploaded file must be a script. Script perform post workload actions."
		},
		"pidgetter_script_path": {
			"name": "PidGetter script path",
			"regex" : "^[a-zA-Z0-9_]+(\\.)sh$",
			"require": true,
			"information_help": "Uploaded file must be a script. Script get pid used by probes."
		}
	},
	"parameters": {
		"watchdog_dur": {
			"type": "integer",
			"default": 30000,
			"condition": {"minValue": 0, "maxValue": 100000, "step": 1},
			"require": true
		}
	},
	"information_help": "Campaign used for Hardware fault injection using script files."
}