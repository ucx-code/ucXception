--- ./20.c
+++ ./20.c._WAEP_4	2016-12-08 00:50:09.403084316 +0000
@@ -89,7 +89,7 @@
 void check_methodParams(prog_env* prog, char* name, is_node* node)
 {
 while (node != NULL){
-insert_method_elements(prog, name, sem_type[node->child->type], node->child->next->id, 1);
+insert_method_elements(prog, name, sem_type, node->child->next->id, 1);
 node = node->next;
 }
 }
