--- ./7.c
+++ ./7.c._MLPA_1	2016-12-08 00:51:06.171083299 +0000
@@ -15,8 +15,6 @@
             for (i = errpos;i < length;i++){
                 word[i] = word[i + 1];
             }
-            errpos = perfect(word);
-            length = strlen(word);
         }
         printf("%s\n", word);
     }
