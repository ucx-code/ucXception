--- ./32.c
+++ ./32.c._MLPA_9	2016-12-08 00:50:45.643083667 +0000
@@ -75,9 +75,6 @@
 {
     short rot, lvl, size, i;
     while (scanf("%hd %hd", &lvl, &rot) != EOF){
-        rot = rot % 4;
-        size = pow(2, lvl);
-        gerarMatrix(lvl, rot);
         for (i = 0;i < size;i++){
             matrix[i][size] = '\0';
             printf("%s\n", matrix[i]);
