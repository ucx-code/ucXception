--- ./10.c
+++ ./10.c._MLPA_157	2016-12-08 00:49:33.875084952 +0000
@@ -239,8 +239,6 @@
     break;
 case Id:
     glob = verifyGlobal(expr->id, prog, method_name);
-    expr_type = get_type(expr->id, prog, method_name);
-    counter++;
     sprintf(c, "%%%d", counter);
     if (!glob) 
         printf("\t%%%d = load %s* %%%s\n", counter, expr_type, expr->id);
