--- ./2.c
+++ ./2.c._WPFV_4	2016-12-08 00:50:06.419084369 +0000
@@ -43,7 +43,7 @@
                 memcpy(matrix + (halfSquare * 2 * halfSquare) + ((i * 2 + 1) * halfSquare), tmp + halfSquare * i, halfSquare);
             }
         }
-        rec(n + 1, target, r);
+        rec(n + 1, n, r);
     }
 }
 
@@ -60,3 +60,13 @@
         }
     }
 }
+, pow(2,n));
+			tmp[(int)pow(2,n)] = '\0';			
+			//matrix[(int)(i*(pow(2,n)+1))] = '\n';
+				printf("%s\n", tmp);
+			//fputs(tmp, stdout);
+			//fwrite(matrix+(int)(i*pow(2,n)), 1, pow(2,n)+1, stdout);
+		}
+	}
+//	fwrite(matrix, 1, 40, stdout);
+}
