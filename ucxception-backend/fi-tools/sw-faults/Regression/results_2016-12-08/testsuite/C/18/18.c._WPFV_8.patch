--- ./18.c
+++ ./18.c._WPFV_8	2016-12-08 00:50:00.395084477 +0000
@@ -1,6 +1,3 @@
-#include <stdio.h>
-#include <math.h>
-#include <limits.h>
 int distancias[13][15];
 float supermarkets[13][2];
 int maxCap;
@@ -32,7 +29,7 @@
         return;
     }
     if (v != 0) 
-        shortestPath(0, len + distancias[v][0], maxCap, bound);
+        shortestPath(0, len + distancias[v][0], i, bound);
 
     for (i = 1;i <= n;i++){
         if (solution[i] == 0) {
