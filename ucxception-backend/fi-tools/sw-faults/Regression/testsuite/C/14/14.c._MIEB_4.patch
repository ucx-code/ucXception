--- 14.c
+++ 14.c._MIEB_4	2016-03-30 00:40:18.154484700 +0100
@@ -126,12 +126,6 @@
 {
     is_node* node = (is_node*)malloc(sizeof (is_node));
     if (stat != NULL) {
-        if (stat->next != NULL) {
-            node->child = stat;
-            node->next = NULL;
-            node->type = CompoundStat;
-            node->id = NULL;
-        }else
             return stat;
     }else
         return NULL;
