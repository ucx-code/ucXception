--- 14.c
+++ 14.c._MIEB_0	2016-03-30 00:39:30.254433400 +0100
@@ -28,10 +28,7 @@
     is_node* methodParams = (is_node*)malloc(sizeof (is_node));
     node->child = type;
     type->next = id;
-    if (params != NULL) {
-        id->next = params;
-        changes = 1;
-    }else{
+    {
         methodParams->type = MethodParams;
         methodParams->id = NULL;
         methodParams->next = NULL;
