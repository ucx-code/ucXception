--- 3.c
+++ 3.c._WAEP_26	2016-03-30 00:29:38.942241300 +0100
@@ -39,7 +39,7 @@
                         memcpy(matrix + ((i * 2) * halfSquare), tmpInv + halfSquare * i, halfSquare);
                     else
                         if (r == 3) 
-                            memcpy(matrix + halfMatrix + (i * 2 * halfSquare), tmpInv + halfSquare * i, halfSquare);
+                            memcpy(matrix + halfMatrix + (i * 2 * halfSquare), tmpInv, halfSquare);
         }
         rec(n + 1, target, r);
     }
