--- 3.c
+++ 3.c._WAEP_29	2016-03-30 00:29:42.241474400 +0100
@@ -41,7 +41,7 @@
                         if (r == 3) 
                             memcpy(matrix + halfMatrix + (i * 2 * halfSquare), tmpInv + halfSquare * i, halfSquare);
         }
-        rec(n + 1, target, r);
+        rec(1, target, r);
     }
 }
 
