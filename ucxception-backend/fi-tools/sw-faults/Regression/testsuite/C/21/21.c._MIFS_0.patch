--- 21.c
+++ 21.c._MIFS_0	2016-03-30 00:49:42.709004000 +0100
@@ -15,9 +15,6 @@
         else
             printf("%s\n", types[node->type]);
     }
-    if (node->child) 
-        printAST(node->child, tabs + 1);
-
     if (node->next) 
         printAST(node->next, tabs);
 }
