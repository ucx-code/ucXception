--- 31.c
+++ 31.c._WPFV_23	2016-03-30 00:59:31.916890900 +0100
@@ -56,7 +56,7 @@
                         memcpy(&tab[i][comp], &tab[i][0], comp * sizeof (char));
                         memcpy(&tab[i + comp][comp], &tab[i][0], comp * sizeof (char));
                     }
-                    invert(comp, 0, comp);
+                    invert(comp, 0, i);
                 }
 
     if (comp * 2 == pow(2, N)) 
