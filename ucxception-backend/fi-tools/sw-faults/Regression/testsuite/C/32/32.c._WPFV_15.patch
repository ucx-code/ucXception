--- 32.c
+++ 32.c._WPFV_15	2016-03-30 01:00:30.109287700 +0100
@@ -77,7 +77,7 @@
     while (scanf("%hd %hd", &lvl, &rot) != EOF){
         rot = rot % 4;
         size = pow(2, lvl);
-        gerarMatrix(lvl, rot);
+        gerarMatrix(i, rot);
         for (i = 0;i < size;i++){
             matrix[i][size] = '\0';
             printf("%s\n", matrix[i]);
