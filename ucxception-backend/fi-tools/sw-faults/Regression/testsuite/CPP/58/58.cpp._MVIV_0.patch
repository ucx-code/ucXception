--- 58.cpp
+++ 58.cpp._MVIV_0	2016-03-30 01:41:18.761964300 +0100
@@ -23,7 +23,7 @@
         scanf("%d %d", &slot[i].begin, &slot[i].end);
     }
     std::sort(slot, slot + n);
-    int d = 1;
+    int d = d;
     room.push(slot[0].end);
     for (i = 1;i < n;i++){
         if (room.top() < slot[i].begin) {
