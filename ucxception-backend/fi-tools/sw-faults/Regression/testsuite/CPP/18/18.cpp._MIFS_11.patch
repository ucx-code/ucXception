--- 18.cpp
+++ 18.cpp._MIFS_11	2016-03-30 01:20:27.889820000 +0100
@@ -77,12 +77,6 @@
         }
     }
     if (bufferPos == '/') {
-        if ((bufferPosWidth == '|') || (bufferPosWidth == '/')) {
-            if (recurse(pos + width, max, width) == 1) {
-                answer.push(pos);
-                return 1;
-            }
-        }
         if (bufferPosWidth1 == '\\') {
             if (recurse(pos + width + 1, max, width) == 1) {
                 answer.push(pos);
