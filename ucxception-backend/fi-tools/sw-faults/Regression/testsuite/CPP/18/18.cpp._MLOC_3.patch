--- 18.cpp
+++ 18.cpp._MLOC_3	2016-03-30 01:20:30.547157800 +0100
@@ -77,7 +77,7 @@
         }
     }
     if (bufferPos == '/') {
-        if ((bufferPosWidth == '|') || (bufferPosWidth == '/')) {
+        if ((bufferPosWidth == '|')) {
             if (recurse(pos + width, max, width) == 1) {
                 answer.push(pos);
                 return 1;
