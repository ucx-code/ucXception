--- 48.cpp
+++ 48.cpp._MIFS_6	2016-03-30 01:39:06.850031500 +0100
@@ -66,9 +66,6 @@
     if (bufferPos == '|') {
         if (bufferPosWidth == '/') {
             if (recurse(pos - width, max, width) == 1) {
-                if (recurseSize != 0) 
-                    strcat2((char*)",", 1);
-
                 sprintf(tmp, "(%d,%d)", getY(pos, width), getX(pos, width));
                 strcat2(tmp);
                 return 1;
