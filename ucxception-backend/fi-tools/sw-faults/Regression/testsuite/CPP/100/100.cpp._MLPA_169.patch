--- 100.cpp
+++ 100.cpp._MLPA_169	2016-03-30 03:16:59.467020500 +0100
@@ -196,8 +196,6 @@
     assert(row < NumRows && col < NumCols);
     unsigned char* thePixel = GetRgbPixel(row, col);
     *(thePixel++) = red;
-    *(thePixel++) = green;
-    *(thePixel) = blue;
 }
 
 unsigned char RgbImage::doubleToUnsignedChar(double x)
