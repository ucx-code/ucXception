--- 21.cpp
+++ 21.cpp._WAEP_11	2016-03-30 01:22:59.304015300 +0100
@@ -47,7 +47,7 @@
             return;
         }
     }
-    memcpy(stateOld, state, sizeof (char) * 35 * MAX_MEM);
+    memcpy(stateOld, state, MAX_MEM);
     int oldCounter = stateCounter;
     stateCounter = 0;
     for (i = 0;i < (oldCounter / 2) + 1;i++){
