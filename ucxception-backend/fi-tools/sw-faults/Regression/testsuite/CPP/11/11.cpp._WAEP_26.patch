--- 11.cpp
+++ 11.cpp._WAEP_26	2016-03-30 01:15:45.568131800 +0100
@@ -40,7 +40,7 @@
                         memcpy(matrix + ((i * 2) * halfSquare), tmp + halfSquare * i, halfSquare);
                     else
                         if (r == 3) 
-                            memcpy(matrix + halfMatrix + (i * 2 * halfSquare), tmp + halfSquare * i, halfSquare);
+                            memcpy(matrix + halfMatrix + (i * 2 * halfSquare), tmp, halfSquare);
         }
         rec(n + 1, target, r);
     }
