--- 1.cpp
+++ 1.cpp._MLPA_0	2016-03-30 01:08:46.592813900 +0100
@@ -49,8 +49,6 @@
     if (buffer[pos] == '\\') {
         if ((buffer[pos + 1] == '/') && (buffer[pos + 2] == '\\')) {
             if (recurse(pos + 2, max, width) == 1) {
-                answer.push(pos + 1);
-                answer.push(pos);
                 return 1;
             }
         }
