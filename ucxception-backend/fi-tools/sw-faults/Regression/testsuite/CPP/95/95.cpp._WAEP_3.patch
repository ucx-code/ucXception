--- 95.cpp
+++ 95.cpp._WAEP_3	2016-03-30 02:03:11.572304000 +0100
@@ -40,7 +40,7 @@
         bipartite = false;
         for (i = 0;i < n;i++){
             memset(mark, false, sizeof (bool) * 500);
-            memset(color, 0, sizeof (int) * 500);
+            memset(color, 0, 500);
             if (dfs(i, 1) == true) 
                 bipartite = true;
         }
