--- 96.cpp
+++ 96.cpp._MLPA_6	2016-03-30 02:03:30.163354600 +0100
@@ -47,11 +47,9 @@
                 printf("NO\n");
                 goto jump;
             }
-        printf("NOT SURE\n");
         jump:
         for (i = 0;i <= n;i++)
             G[i].clear();
-        memset(partition, 0, sizeof (int) * (n + 1));
         memset(visited, 0, sizeof (int) * (n + 1));
     }
     return 0;
