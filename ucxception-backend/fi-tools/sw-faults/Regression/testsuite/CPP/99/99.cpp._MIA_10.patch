--- 99.cpp
+++ 99.cpp._MIA_10	2016-03-30 02:41:57.002218800 +0100
@@ -892,7 +892,7 @@
     y = (2.0 * ((GLfloat)rand()) / ((GLfloat)RAND_MAX)) - 1.0;
     z = (2.0 * ((GLfloat)rand()) / ((GLfloat)RAND_MAX)) - 1.0;
     len = sqrt(x * x + y * y + z * z);
-    if (len) {
+    {
         x = x / len;
         y = y / len;
         z = z / len;
