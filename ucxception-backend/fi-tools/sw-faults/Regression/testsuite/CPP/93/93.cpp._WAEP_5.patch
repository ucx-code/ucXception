--- 93.cpp
+++ 93.cpp._WAEP_5	2016-03-30 02:02:38.150498900 +0100
@@ -36,7 +36,7 @@
         }
         is = true;
         memset(marked, 0, sizeof (bool) * n);
-        memset(color, 0, sizeof (bool) * n);
+        memset(color, 0, n);
         for (i = 0;i < n;i++){
             if (!marked[i]) 
                 dfs(i);
