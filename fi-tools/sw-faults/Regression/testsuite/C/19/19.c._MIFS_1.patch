--- 19.c
+++ 19.c._MIFS_1	2016-03-30 00:45:44.746237600 +0100
@@ -34,10 +34,6 @@
     if ((aux + (bound - distancias[v][18]) / 2) >= best) 
         return;
 
-    if (isSolution() && aux < best) {
-        best = aux;
-        return;
-    }
     if (v != 0) 
         shortestPath(0, len + distancias[v][0], maxCap, bound);
 
