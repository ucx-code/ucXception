--- 10.c
+++ 10.c._MLPA_156	2016-03-30 00:37:12.992115700 +0100
@@ -238,8 +238,6 @@
 
     break;
 case Id:
-    glob = verifyGlobal(expr->id, prog, method_name);
-    expr_type = get_type(expr->id, prog, method_name);
     counter++;
     sprintf(c, "%%%d", counter);
     if (!glob) 
