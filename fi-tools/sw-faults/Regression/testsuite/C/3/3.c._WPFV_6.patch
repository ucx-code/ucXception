--- 3.c
+++ 3.c._WPFV_6	2016-03-30 00:29:39.337735600 +0100
@@ -41,7 +41,7 @@
                         if (r == 3) 
                             memcpy(matrix + halfMatrix + (i * 2 * halfSquare), tmpInv + halfSquare * i, halfSquare);
         }
-        rec(n + 1, target, r);
+        rec(n + 1, quickPow, r);
     }
 }
 
