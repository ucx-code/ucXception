--- 31.c
+++ 31.c._MFC_16	2016-03-30 00:59:28.319934500 +0100
@@ -56,7 +56,7 @@
                         memcpy(&tab[i][comp], &tab[i][0], comp * sizeof (char));
                         memcpy(&tab[i + comp][comp], &tab[i][0], comp * sizeof (char));
                     }
-                    invert(comp, 0, comp);
+                    ;
                 }
 
     if (comp * 2 == pow(2, N)) 
