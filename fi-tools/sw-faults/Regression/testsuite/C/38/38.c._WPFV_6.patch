--- 38.c
+++ 38.c._WPFV_6	2016-03-30 01:02:48.840179700 +0100
@@ -15,7 +15,7 @@
         scanf("%d %d", &begin[i], &endTime[i]);
     }
     qsort(begin, n, sizeof (int), sorting);
-    qsort(endTime, n, sizeof (int), sorting);
+    qsort(endTime, endTime, sizeof (int), sorting);
     for (i = 1;i < n;i++){
         if (endTime[j] > begin[i]) {
             k++;
