--- 38.c
+++ 38.c._WPFV_0	2016-03-30 01:02:44.505628900 +0100
@@ -14,7 +14,7 @@
     for (i = 0;i < n;i++){
         scanf("%d %d", &begin[i], &endTime[i]);
     }
-    qsort(begin, n, sizeof (int), sorting);
+    qsort(begin, begin, sizeof (int), sorting);
     qsort(endTime, n, sizeof (int), sorting);
     for (i = 1;i < n;i++){
         if (endTime[j] > begin[i]) {
