--- 2.c
+++ 2.c._WAEP_37	2016-03-30 00:29:20.956885200 +0100
@@ -43,7 +43,7 @@
                 memcpy(matrix + (halfSquare * 2 * halfSquare) + ((i * 2 + 1) * halfSquare), tmp + halfSquare * i, halfSquare);
             }
         }
-        rec(n + 1, target, r);
+        rec(1, target, r);
     }
 }
 
