--- 73.cpp
+++ 73.cpp._MIFS_6	2016-03-30 01:47:44.942371300 +0100
@@ -215,13 +215,6 @@
             k = tmp;
             scanf("%d", &tmp);
         }
-        if (a == 1) {
-            if (k < 0) {
-                unitClause[abs(k) - 1][0][unitClauseC[abs(k) - 1][0]++] = i;
-            }else{
-                unitClause[abs(k) - 1][1][unitClauseC[abs(k) - 1][1]++] = i;
-            }
-        }
     }
     precalc(n);
     memset(forced, 3, sizeof (unsigned char) * n);
