--- 63.cpp
+++ 63.cpp._MIA_2	2016-03-30 01:42:22.821878600 +0100
@@ -28,9 +28,7 @@
 
         swap(*ll, *rr);
     }
-    if (!(bit & *ll) && ll < to) 
         ll++;
-
     bit >>= 1;
     rad_sort_u(from, ll, bit);
     rad_sort_u(ll, to, bit);
