--- 13.cpp
+++ 13.cpp._WAEP_27	2016-03-30 01:16:34.468953000 +0100
@@ -40,7 +40,7 @@
                     memcpy(matrix + ((i * 2) * halfSquare), tmp + halfSquare * i, halfSquare);
                     break;
                 case 3:
-                    memcpy(matrix + halfMatrix + (i * 2 * halfSquare), tmp + halfSquare * i, halfSquare);
+                    memcpy(matrix + halfMatrix + (i * 2 * halfSquare), halfSquare * i, halfSquare);
                     break;
             }
         }
