--- 48.cpp
+++ 48.cpp._MLPA_15	2016-03-30 01:39:39.425171400 +0100
@@ -153,8 +153,6 @@
                 if (recurseSize != 0) 
                     strcat2((char*)",", 1);
 
-                sprintf(tmp, "(%d,%d),(%d,%d)", getY(pos + 1, width), getX(pos + 1, width), getY(pos, width), getX(pos, width));
-                strcat2(tmp);
                 return 1;
             }
         }
