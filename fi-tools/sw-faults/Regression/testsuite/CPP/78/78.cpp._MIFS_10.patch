--- 78.cpp
+++ 78.cpp._MIFS_10	2016-03-30 01:51:52.448777700 +0100
@@ -164,13 +164,6 @@
             k = tmp;
             scanf("%d", &tmp);
         }
-        if (a == 1) {
-            if (k < 0) {
-                unitClause[abs(k) - 1][0][unitClauseC[abs(k) - 1][0]++] = i;
-            }else{
-                unitClause[abs(k) - 1][1][unitClauseC[abs(k) - 1][1]++] = i;
-            }
-        }
     }
     printf("\n");
     unitPropagation();
