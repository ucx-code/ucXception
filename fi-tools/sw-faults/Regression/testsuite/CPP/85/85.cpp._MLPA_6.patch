--- 85.cpp
+++ 85.cpp._MLPA_6	2016-03-30 01:58:40.799148000 +0100
@@ -146,8 +146,6 @@
             if (tmpEstado[a] == 0) 
                 friendPerIngredient[i] += friendship[a];
     }
-    memcpy(c2, c, sizeof (unsigned char) * n);
-    qsort(c2, n, sizeof (unsigned char), compare);
     precalc(n);
     rec(c, 0, n);
     printf("%d\n", best);
