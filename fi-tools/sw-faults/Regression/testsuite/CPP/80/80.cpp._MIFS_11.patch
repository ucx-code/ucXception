--- 80.cpp
+++ 80.cpp._MIFS_11	2016-03-30 01:53:01.142640400 +0100
@@ -194,8 +194,6 @@
             c[i] = rand() % 2;
         }
         int tmp = calc(c, n);
-        if (tmp < best) 
-            best = tmp;
     }
     memset(c, 0, sizeof (unsigned char) * n);
     for (i = 1;i < n;i++)
