--- 100.cpp
+++ 100.cpp._MVAE_2	2016-03-30 03:14:20.998829500 +0100
@@ -54,7 +54,6 @@
     if (!ImagePtr) {
         fprintf(stderr, "Unable to allocate memory for %ld x %ld bitmap: %s.\n", NumRows, NumCols, filename);
         Reset();
-        ErrorCode = MemoryError;
         fclose(infile);
         return false;
     }
