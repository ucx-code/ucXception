--- 100.cpp
+++ 100.cpp._MLAC_0	2016-03-30 03:13:37.592856900 +0100
@@ -32,7 +32,7 @@
     bool fileFormatOK = false;
     int bChar = fgetc(infile);
     int mChar = fgetc(infile);
-    if (bChar == 'B' && mChar == 'M') {
+    if (mChar == 'M') {
         skipChars(infile, 4 + 2 + 2 + 4 + 4);
         NumCols = readLong(infile);
         NumRows = readLong(infile);
