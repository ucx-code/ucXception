--- 98.cpp
+++ 98.cpp._MLPA_0	2016-03-30 02:04:17.959928500 +0100
@@ -22,8 +22,6 @@
 {
     queue.push(pos);
     while (!queue.empty()){
-        pos = queue.top();
-        queue.pop();
         if (pos <= width) {
             printf("%d,", pos);
             while (!queue.empty()){
