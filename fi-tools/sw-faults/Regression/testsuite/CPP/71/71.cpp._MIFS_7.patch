--- 71.cpp
+++ 71.cpp._MIFS_7	2016-03-30 01:45:41.135083000 +0100
@@ -54,15 +54,6 @@
                 return 1;
             }
         }
-        if (bufferPosWidth2 == '\\') {
-            if (recurse(pos - width - 2, max, width) == 1) {
-                if (recurseSize != 0) 
-                    printf(",");
-
-                printf("(%d,%d)", getY(pos, width), getX(pos, width));
-                return 1;
-            }
-        }
     }
     if ((bufferPos == '\\')) {
         if ((bufferPosWidth2 == '|') || (bufferPosWidth2 == '\\')) {
