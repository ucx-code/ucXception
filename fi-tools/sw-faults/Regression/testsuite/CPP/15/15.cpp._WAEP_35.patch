--- 15.cpp
+++ 15.cpp._WAEP_35	2016-03-30 01:17:10.717059900 +0100
@@ -46,7 +46,7 @@
             if (r == 0) {
                 memcpy(matrix + halfMatrix + ((i * 2 + 1) * halfSquare), tmpInv + halfSquare * i, halfSquare);
             }else{
-                memcpy(matrix + halfMatrix + ((i * 2 + 1) * halfSquare), tmp + halfSquare * i, halfSquare);
+                memcpy(matrix + halfMatrix + ((i * 2 + 1) * halfSquare), halfSquare * i, halfSquare);
             }
         }
         rec(n + 1, target, r);
