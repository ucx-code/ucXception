--- 99.cpp
+++ 99.cpp._MVAE_3	2016-03-30 02:41:57.867828900 +0100
@@ -893,7 +893,6 @@
     z = (2.0 * ((GLfloat)rand()) / ((GLfloat)RAND_MAX)) - 1.0;
     len = sqrt(x * x + y * y + z * z);
     if (len) {
-        x = x / len;
         y = y / len;
         z = z / len;
     }
