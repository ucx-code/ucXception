--- 99.cpp
+++ 99.cpp._MLPA_1220	2016-03-30 02:33:25.489965800 +0100
@@ -800,8 +800,6 @@
     glEnd();
     glPopMatrix();
     glDisable(GL_TEXTURE_2D);
-    glEnable(GL_TEXTURE_2D);
-    glBindTexture(GL_TEXTURE_2D, skybox[1]);
     glPushMatrix();
     glNormal3f(0.0f, -1.0f, 0.0f);
     glBegin(GL_QUADS);
