--- 99.cpp
+++ 99.cpp._MLPA_824	2016-03-30 02:21:15.511403000 +0100
@@ -696,8 +696,6 @@
     glEnd();
     glPopMatrix();
     glDisable(GL_TEXTURE_2D);
-    glEnable(GL_TEXTURE_2D);
-    glBindTexture(GL_TEXTURE_2D, texture[4]);
     glPushMatrix();
     glBegin(GL_QUADS);
     glNormal3f(0.0f, 1.0f, 0.0f);
