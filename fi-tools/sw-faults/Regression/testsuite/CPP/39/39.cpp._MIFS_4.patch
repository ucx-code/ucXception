--- 39.cpp
+++ 39.cpp._MIFS_4	2016-03-30 01:36:34.340889000 +0100
@@ -39,9 +39,6 @@
     if (bufferPos == '|') {
         if (bufferPosWidth == '/') {
             if (recurse(pos - width, max, width) == 1) {
-                if (recurseSize != 0) 
-                    printf(",");
-
                 printf("(%d,%d)", getY(pos, width), getX(pos, width));
                 return 1;
             }
