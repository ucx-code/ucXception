--- 93.cpp
+++ 93.cpp._WAEP_0	2016-03-30 02:02:34.957593600 +0100
@@ -14,7 +14,7 @@
     for (w = 0;w < vizinhosC[v];w++){
         if (marked[vizinhos[v][w]] == false) {
             color[vizinhos[v][w]] = !color[v];
-            dfs(vizinhos[v][w]);
+            dfs(vizinhos[v]);
         }else
             if (color[vizinhos[v][w]] == color[v]) {
                 is = false;
