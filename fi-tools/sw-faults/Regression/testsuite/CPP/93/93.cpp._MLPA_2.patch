--- 93.cpp
+++ 93.cpp._MLPA_2	2016-03-30 02:02:35.720190200 +0100
@@ -34,9 +34,6 @@
             vizinhos[a][vizinhosC[a]++] = b;
             vizinhos[b][vizinhosC[b]++] = a;
         }
-        is = true;
-        memset(marked, 0, sizeof (bool) * n);
-        memset(color, 0, sizeof (bool) * n);
         for (i = 0;i < n;i++){
             if (!marked[i]) 
                 dfs(i);
