--- 7.cpp
+++ 7.cpp._MLPA_0	2016-03-30 01:13:35.423831000 +0100
@@ -58,8 +58,6 @@
     int n, r;
     int i, squared;
     while (scanf("%d %d", &n, &r) != EOF){
-        squared = quickPow[n];
-        rec(0, n, r % 4);
         for (i = 0;i < squared;i++){
             if (n % 2 == 0) {
                 memcpy(tmp2, matrix2 + (i * squared), squared);
