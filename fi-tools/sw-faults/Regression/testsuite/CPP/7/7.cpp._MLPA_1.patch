--- 7.cpp
+++ 7.cpp._MLPA_1	2016-03-30 01:13:40.419466300 +0100
@@ -62,8 +62,6 @@
         rec(0, n, r % 4);
         for (i = 0;i < squared;i++){
             if (n % 2 == 0) {
-                memcpy(tmp2, matrix2 + (i * squared), squared);
-                tmp2[squared] = '\0';
                 printf("%s\n", tmp2);
             }else{
                 memcpy(matrix2, tmp2 + (i * squared), squared);
