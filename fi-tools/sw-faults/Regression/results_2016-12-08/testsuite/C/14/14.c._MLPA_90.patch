--- ./14.c
+++ ./14.c._MLPA_90	2016-12-08 00:49:48.635084688 +0000
@@ -198,10 +198,6 @@
 is_node* insertST_id_assign_exp_sem(is_node* id, is_node* expr)
 {
     is_node* node = (is_node*)malloc(sizeof (is_node));
-    node->child = id;
-    id->next = expr;
-    node->type = Store;
-    node->id = NULL;
     return node;
 }
 
