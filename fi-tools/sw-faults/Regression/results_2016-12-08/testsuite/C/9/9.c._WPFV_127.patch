--- ./9.c
+++ ./9.c._WPFV_127	2016-12-08 00:51:12.387083188 +0000
@@ -1,6 +1,3 @@
-#include <stdio.h>
-#include <stdlib.h>
-#include <string.h>
 int checkNumber(int x);
 void printTable(int** table, int lines, int columns);
 int** copyTable(int** table, int lines, int columns);
@@ -32,7 +29,7 @@
     printf("\n");
     printTable(tableLines, lines, columns);
     printf("\n");
-    printTable(tableColumns, lines, columns);
+    printTable(tableColumns, lines, *);
     for (i = 0;i < lines;i++){
         free(table[i]);
         free(tableLines[i]);
