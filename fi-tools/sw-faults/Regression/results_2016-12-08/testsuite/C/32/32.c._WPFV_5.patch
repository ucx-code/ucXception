--- ./32.c
+++ ./32.c._WPFV_5	2016-12-08 00:50:45.123083676 +0000
@@ -10,7 +10,7 @@
         matrix[0][0] = '0';
     }else{
         gerarMatrix(lvl - 1, rot);
-        aux1 = pow(2, lvl);
+        aux1 = pow(2, rot);
         aux2 = pow(2, (lvl - 1));
         if (rot == 0) {
             for (i = 0;i < aux1;i++){
