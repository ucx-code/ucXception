--- ./20.c
+++ ./20.c._MFC_14	2016-12-08 00:50:10.291084300 +0000
@@ -172,7 +172,7 @@
 while (node != NULL){
     switch (node->type){
         case CompoundStat:
-            semantic_statements(prog, name, node->child);
+            ;
             break;
         case IfElse:
             expr_type = expr_recursive(prog, name, node->child);
