--- ./31.c
+++ ./31.c._WPFV_22	2016-12-08 00:50:43.283083709 +0000
@@ -56,7 +56,7 @@
                         memcpy(&tab[i][comp], &tab[i][0], comp * sizeof (char));
                         memcpy(&tab[i + comp][comp], &tab[i][0], comp * sizeof (char));
                     }
-                    invert(comp, 0, comp);
+                    invert(comp, 0, R);
                 }
 
     if (comp * 2 == pow(2, N)) 
