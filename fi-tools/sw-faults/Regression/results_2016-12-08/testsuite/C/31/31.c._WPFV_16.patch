--- ./31.c
+++ ./31.c._WPFV_16	2016-12-08 00:50:43.163083711 +0000
@@ -49,7 +49,7 @@
                     memcpy(&tab[i + comp][0], &tab[i][0], comp * sizeof (char));
                     memcpy(&tab[i + comp][comp], &tab[i][0], comp * sizeof (char));
                 }
-                invert(0, 0, comp);
+                invert(0, 0, R);
             }else
                 if (R == 3) {
                     for (i = 0;i < comp;i++){
